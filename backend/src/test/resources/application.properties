# Test Configuration - MySQL Database

# Application Name
spring.application.name=fyp-management-system-test

# MySQL Database Configuration for Testing
spring.datasource.url=jdbc:mysql://localhost:3306/fyp_management?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Rafeh@0091
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Disable File Upload for Tests
file.upload-dir=./test-uploads

# JWT Configuration
jwt.secret=test-secret-key-for-testing-purposes-only-not-for-production
jwt.expiration=86400000

# Disable Email for Tests
spring.mail.host=localhost
spring.mail.port=25
spring.mail.username=tracksphere.fyp@gmail.com
spring.mail.password=cpni bkan avh zfvq
# Logging
logging.level.org.springframework=ERROR
logging.level.com.fyp.management=ERROR

